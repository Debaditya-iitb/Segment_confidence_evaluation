# Install torch FIRST, from the CUDA index matching your driver, before anything
# here. The reference run used CUDA 12.1:
#   pip install torch==2.3.1 --index-url https://download.pytorch.org/whl/cu121
# CPU-only is fine too (slower, and see docs/REPRODUCIBILITY.md on CPU vs GPU):
#   pip install torch==2.3.1

transformers==4.48.0
librosa==0.11.0
numpy==1.26.4          # keep <2.0: transformers 4.48 + numba pin against it
pandas==2.3.3
tqdm==4.67.1
rapidfuzz==3.14.3
soundfile==0.13.1

# pulled in by the above, pinned because they moved the reference numbers:
tokenizers==0.21.4
huggingface_hub==0.36.0
safetensors==0.7.0
